# ngrok_connect_to_cassandra
Connecting to local cassandra using ngrok .  ngrok will expose our local host to internet through a domain. Using which we can connect to our local host as a remote server.
